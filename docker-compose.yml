version: '3.8'

services:
  mlops_backend:
    build:
      context: .
      dockerfile: Dockerfile.fastapi
    ports:
      - "8001:8001"

  mlops_frontend:
    build:
      context: .
      dockerfile: Dockerfile.streamlit
    ports:
      - "8501:8501"
    depends_on:
      - mlops_backend